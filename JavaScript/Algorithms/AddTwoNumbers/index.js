/**
 * Definitoin for singly-linked list
 * function ListNode(val) {
 *     this.val = val;
 *     this.next = null;
 * }
 */

let addTwoNumbers = function (l1, l2) {
  let ListNode = function (val) {
    this.val = val;
    this.next = null;
  };
};

addTwoNumbers();
